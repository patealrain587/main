# 중력부양선 설계 인수인계 (RW-GRAV-21)

림월드 1.6 Odyssey 중력부양선 내부 배치 도면을 만드는 작업입니다. 새 세션은 이 문서를 먼저 읽고 이어서 작업하세요.

- 공개 도면(아티팩트, 비공개): https://claude.ai/artifact/PVWrC3oCVJYmtjPoEbmbnG — 같은 주소를 이어서 고칠 때는 Artifact 도구로 먼저 `read` 한 뒤 `url` 을 넘겨 publish.
- 기준 모드 목록: 260924-11 경량 OP 번역 (317개, RT Fuse 제외). `data/` 의 CSV·MD 는 그 전 목록(260924-10) 기준이라, RT Fuse 말고 빠진 모드가 있으면 해당 건물을 확인해야 함.

## 파일

| 파일 | 내용 |
|---|---|
| `grav.py` | 배치 정의 + 검증기. 방·통로·갑판 좌표, 건물 배치, 온도·산소, 격벽, 전선 3망, 모든 검사. 오류가 있으면 목록을 찍고 종료 코드 1 |
| `page_template.html` | 도면 페이지 틀. `__DATA__` 자리에 grav.json 이 들어감. 설명 카드 문구도 여기 |
| `build.py` | `python3 build.py` → `grav.json`, `gravship-plan.html` 생성 (몇 초) |
| `grav.json` / `gravship-plan.html` | 현재 결과물 (v21) |
| `data/facilities.csv` | 건물 크기·작업칸·전력·설치 조건·모드 설정값 (823개) |
| `data/combat.csv` | 포탑·보호막 성능, 파괴 시 폭발 반경 |
| `data/factions.csv`, `unused_factions.csv`, `floors.csv` 와 MD 4개 | 사용자가 올린 조사 자료 |

좌표: 선체 좌상단 기준 로컬 좌표, 그리드 오프셋 `OX, OY = 40, 16`, 크기 `GW, GH = 252, 176`. 방은 안쪽 칸 사각형이고 벽은 자동 생성.

## 사용자 요구 (지켜야 할 것)

- 건물 크기·전력·설치 조건은 facilities.csv 값. NotUnderRoof 건물은 갑판, InvalidOverSubstructure 건물은 제외. 추진기 뒤 금지구역과 작업칸을 막지 않기.
- 정착민 5~20명, 메이드·밀리언 수십 기. 밀리라 작업대 전부 + 메이드용 대형 메카 배양기. 자동 공허 채굴기 여러 대. 농장 생산량은 바닐라보다 높음. 메디포드라 치료실은 작게. 수감 공간은 적당히.
- 동선이 최우선, 외곽 모양은 자유(순수 삼각형은 싫어함). 공실은 적당히, **외곽에만**. 대형 모드 2개 자리.
- 구역: 창고는 작업실 안에 적재함 1~2기(대형 적재함 1,000스택), 같은 재료 작업대끼리 한 방. 수감 홀 둘레에 냉동고·재단·화학·치료·유전자. 시체·부산물 가공 함께. 약품실은 농장 옆, 채굴은 제조 옆, 메카 제작은 일반 제조 옆, 충전은 단순작업 옆, 침실은 고급 작업 옆. 연료실은 화학 제작 근처(진입 지점과는 떨어지게). 조종실·구동기 중앙. 보호막실은 간격 두고. 실체 연구는 대형 보관실과 한 구역. 간부실 2개.
- 방어: 폭발 반경이 있는 포탑·전투기는 그 반경 안에 방도, 다른 건물도 없게 띄움(연쇄 폭발 금지). 여유 공간은 좋음. 셀레테크 포탑 섞기. 소형 보호막은 폭발 이격 예외.
- 습격은 동쪽 진입로 한 곳으로 유도, 그 뒤 완충 구역(킬존 회랑 + 경비실), 정착민용 옆길.
- 고급 보호막(GravTech)은 아군 탄도 막음 → 모든 포탑·전투기는 고급 보호막 안.
- 킬존 벽은 엄폐 없는 **중력선 진공 벽 총안**(적도 벽에 붙어 엄폐 못 받음, 기밀). 경비실 안쪽에 엄폐물.
- 전력: **매립 전선만**. 대형 축전지 없음(누전은 일반 전선만 대상). 자기 보호막 발생기 1대(기계실, 반응로와 같은 망, 냉각 파일런). 전선을 과하게 늘리지 말 것 — 이중화 집착 금지.
- **방어 포탑은 평소 꺼 둠 → 스위치 1개로 한 번에 끄고 켬.** 대공·요격·위상 배열 레이더는 상시.
- 무선 전력(Power++) 쓰지 않음(모드를 뺄 수 있음). 에어록 바깥 문은 무전력 문(초월공학 문).

## 모드 설정값 (도면이 쓰는 값)

- 중력장 확장기 구동기 거리 100 (Bigger Gravships, 정의 18.9), 확장기·반응로 하부구조 반경 100으로 계산.
- 특이점 반응로 500,000 W, 지지량 5,000, 최대 20개 (정의: 150,000 W, 최대 1개, **구동기 거리 29.9는 설정 미확인이라 지킴**).
- 고급 보호막 반경 80 (정의 44.9), 최대 20기. 보호막 파일런 반경 20.
- 중력 코어 배터리 100,000 Wd (설정) — 현재 도면에는 없음.
- 전력 설비는 약 6칸 안 가장 가까운 전선 하나에 자동 연결(바닐라 기억 기준), 전선끼리는 상하좌우로 맞닿아야 합쳐짐.

## 현재 상태 (v21)

- **맵 크기 제약: 일반 맵 180×180.** 하부구조 가로 171 × 세로 158 (x 0..170, y −13..144), 추진기 뒤 금지구역 포함 세로 167 (y −13..153). 더 넓히면 맵을 벗어남.
- 추진기 갑판은 남쪽(배기 남쪽), 동쪽은 폭 22칸 안에 격납 갑판 / 방어 구획 / 방어 진입 갑판(남쪽 입구 진입로)을 세로로 쌓음.

- 하부구조 23,888칸 / 지지 30,000 (구동기 + 확장기 2 + 반응로 3). 빈틈 없음.
- 발전 1,571,720 W (반응로 3기 + 암흑 물질 축전지 11기 등), 사용 272,205 W, 방어 스위치 끄면 −69,390 W.
- 포탑 58기, 전투기 8기, 소형 보호막 45기, 고급 보호막 4기(선체 100%, 갑판 99.9%), 폭발 쌍 검사 910쌍 통과, 진입로 열림, 킬존 지그재그 48칸.
- 전선 3망:
  - **본선(상시)**: 통로 중심선 한 줄 + 교차점 우회 8칸, 남쪽 전력실 모선(3갈래), 방어 구획 급전 A/B + 고리(암흑 물질 4기).
  - **방어망(빨강)**: 선체 바깥 C자 경로(서쪽은 외벽 x=0 밑). 방어 설비실 주 스위치(130,117) 한 칸으로만 본선과 이어짐. 갑판 전력 포탑·소형 보호막·사격 통제 레이더·진입로 진공 벽.
  - **대공 독립망(하늘색)**: 북측(암흑 물질 5)·남측(2) 포대의 FLS-03, 위상 배열 레이더, 레이저 포인트 방어 + 옆 소형 보호막.
  - 격납 갑판, 센서 갑판, 경비실 근거리 포탑은 본선(상시). 밀리라 포탑은 전력 없음.
  - 태양 레이저 포탑(+30 W)은 방어망에 붙어 스위치 꺼도 약 240 W 남음(알려진 한계).

## 남은 일 / 아이디어

- 가로 통로 1, 세로 통로 1·2 중간 킬존(옆 방 2~3줄 축소 필요) — 사용자 답변 대기였음.
- 맵 폭 때문에 동쪽 확장은 약 9칸, 세로는 약 13칸 여유뿐. 그리핀 전투기 자리 없음.
- 식탁·의자·조명 등 가구는 도면에서 빠져 있음.
- 게임에서 확인할 값: 연결 거리 6칸과 동거리 우선순위, 반응로 거리 제한 설정, 고급 보호막 반경 설정, 진공 지붕·드롭 포드 동작.

## 작업 방식 메모

- 사용자는 한국어. 스크린샷 반복은 싫어함(필요할 때만). 큰 구조 변경 전에는 선택지를 짧게 보여 주고 고르게 함.
- 변경할 때마다 `build.py` 로 검증 통과 확인 → 설명 카드 문구(`page_template.html`) 맞추기 → 문서 번호(RW-GRAV-nn) 올리기 → 아티팩트 재게시.
