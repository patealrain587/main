# 중력부양선 설계 인수인계 (RW-GRAV-30)

림월드 1.6 Odyssey 중력부양선 내부 배치 도면을 만드는 작업입니다. 새 세션은 이 문서를 먼저 읽고 이어서 작업하세요.

- 이 폴더(v30) 도면: https://claude.ai/artifact/7rvVt3FjLN5jKH2w284gPH — 이어서 고칠 때는 Artifact 도구로 먼저 `read` 한 뒤 `url` 을 넘겨 publish.
- 원래 도면 주소 https://claude.ai/artifact/PVWrC3oCVJYmtjPoEbmbnG 에는 다른 세션이 이어 간 RW-GRAV-23(포탑 109기, 가로 216칸 — 180맵에 안 들어감)이 올라가 있음. 이 폴더와는 다른 갈래이니 덮어쓰지 말 것.
- 기준 모드 목록: 260924-11 경량 OP 번역 (317개, RT Fuse 제외). `data/` 의 CSV·MD 는 그 전 목록(260924-10) 기준이라, RT Fuse 말고 빠진 모드가 있으면 해당 건물을 확인해야 함.

## 파일

| 파일 | 내용 |
|---|---|
| `grav.py` | 배치 정의 + 검증기. 방·통로·갑판 좌표, 건물 배치, 온도·산소, 격벽, 전선 3망, 모든 검사. 오류가 있으면 목록을 찍고 종료 코드 1 |
| `page_template.html` | 도면 페이지 틀. `__DATA__` 자리에 grav.json 이 들어감. 설명 카드 문구도 여기 |
| `build.py` | `python3 build.py` → `grav.json`, `gravship-plan.html` 생성 (몇 초) |
| `grav.json` / `gravship-plan.html` | 현재 결과물 (v29) |
| `data/연결_설비_정리.md` | 사용자가 준 연결 설비 표 (대상 137개·연결 1140건, 거리·최대 수·인접 조건). grav.py 가 직접 읽음 |
| `data/facilities.csv` | 건물 크기·작업칸·전력·설치 조건·모드 설정값 (823개) |
| `data/combat.csv` | 포탑·보호막 성능, 파괴 시 폭발 반경 |
| `data/factions.csv`, `unused_factions.csv`, `floors.csv` 와 MD 4개 | 사용자가 올린 조사 자료 |

좌표: 선체 좌상단 기준 로컬 좌표, 그리드 오프셋 `OX, OY = 40, 24`, 크기 `GW, GH = 252, 184`. 방은 안쪽 칸 사각형이고 벽은 자동 생성.

## 사용자 요구 (지켜야 할 것)

- 건물 크기·전력·설치 조건은 facilities.csv 값. NotUnderRoof 건물은 갑판, InvalidOverSubstructure 건물은 제외. 추진기 뒤 금지구역과 작업칸을 막지 않기.
- 정착민 5~20명, 메이드·밀리언 수십 기. 밀리라 작업대 전부 + 메이드용 대형 메카 배양기. 자동 공허 채굴기 여러 대. 농장 생산량은 바닐라보다 높음. 메디포드라 치료실은 작게. 수감 공간은 적당히.
- 동선이 최우선, 외곽 모양은 자유(순수 삼각형은 싫어함). 공실은 적당히, **외곽에만**. 대형 모드 2개 자리.
- 구역: 창고는 작업실 안에 적재함 1~2기(대형 적재함 1,000스택), 같은 재료 작업대끼리 한 방. 수감 홀 둘레에 냉동고·재단·화학·치료·유전자. 시체·부산물 가공 함께. 약품실은 농장 옆, 채굴은 제조 옆, 메카 제작은 일반 제조 옆, 충전은 단순작업 옆, 침실은 고급 작업 옆. 연료실은 화학 제작 근처(진입 지점과는 떨어지게). 조종실·구동기 중앙. 보호막실은 간격 두고. 실체 연구는 대형 보관실과 한 구역. 간부실 2개.
- 방어: 폭발 반경이 있는 포탑·전투기는 그 반경 안에 방도, 다른 건물도 없게 띄움(연쇄 폭발 금지). 여유 공간은 좋음. 셀레테크 포탑 섞기. 소형 보호막은 폭발 이격 예외.
- 습격은 동쪽 진입로 한 곳으로 유도, 그 뒤 완충 구역(킬존 회랑 + 경비실), 정착민용 옆길.
- 고급 보호막(GravTech)은 아군 탄도 막음 → 모든 포탑·전투기는 고급 보호막 안.
- 킬존 벽은 엄폐 없는 **중력선 진공 벽 총안**(적도 벽에 붙어 엄폐 못 받음, 기밀). 경비실 안쪽에 엄폐물.
- 전력: **매립 전선만**. 대형 축전지 없음(누전은 일반 전선만 대상). 자기 보호막 발생기 1대(기계실, 반응로와 같은 망, 냉각 파일런). 전선을 과하게 늘리지 말 것 — 이중화 집착 금지.
- **방어 포탑은 평소 꺼 둠 → 스위치 1개로 한 번에 끄고 켬.** 대공·요격·위상 배열 레이더는 상시.
- 무선 전력(Power++) 쓰지 않음(모드를 뺄 수 있음). 에어록 바깥 문은 무전력 문(초월공학 문).

## 모드 설정값 (도면이 쓰는 값)

- 중력장 확장기 구동기 거리 100 (Bigger Gravships, 정의 18.9), 확장기·반응로 하부구조 반경 100으로 계산.
- 특이점 반응로 500,000 W, 지지량 5,000, 최대 20개 (정의: 150,000 W, 최대 1개, **구동기 거리 29.9는 설정 미확인이라 지킴**).
- 고급 보호막 반경 80 (정의 44.9), 최대 20기. 보호막 파일런 반경 20.
- 중력 코어 배터리 100,000 Wd (설정) — 현재 도면에는 없음.
- 전력 설비는 약 6칸 안 가장 가까운 전선 하나에 자동 연결(바닐라 기억 기준), 전선끼리는 상하좌우로 맞닿아야 합쳐짐.

## 현재 상태 (v29)

- **크기:** 게임에서 지은 벽 기준 하부구조 가로 166 × 세로 136 (x −7..158, y −17..118), 추진기 금지구역 포함 세로 145 (y −17..127). 서쪽 외곽 때문에 가로가 160을 6칸 넘음(180 맵 안). 그리드 오프셋 `OX, OY = 40, 24`, `GW, GH = 252, 184`.
- 좌표는 최종 좌표계로 직접 적음 (`DEL = set()`; 열 삭제 재매핑 기능은 남아 있음).
- 배치: 서쪽 = 위아래 격납 갑판(북서: 전투기·센서, 남서: 왕복선·착륙장·교역·FLS-03·위상 배열 레이더) + 가운데 세 줄 방(세로 서쪽 통로). 코어 = 거주 / 오락·대식당(원래 크기) / 구동기 / 반응로 3기·기계실 / 생산. 동쪽 = 실체·연구(1실) / 수감 허브 / 연료·예비 발전·진입로 정비실. 동쪽 띠(폭 13) = 방어 설비실 → 경비 통로 → 북측 경비실 → 세로 킬존(7×43, 경로 84칸, 출구 가로 통로 1) + 동측 경비실(5×59) → 방어 진입 갑판(남쪽 입구, 진입로 폭 5).
- 남쪽 포대 없음. 남쪽(v29): 금속·부품 작업실 (50,93,28,19)·밀리라 (79,93,19,19) 가 y111까지, 세로 통로 1·2 도 y111까지. 남측 에어록 1 (46,113,9,5)·2 (93,113,9,5) 가 추진기 갑판 (56,113,36,5) 양 끝, 추진기 12기 중심 y116. 남동 공실 (103,106,41,8) 문 (102,109). 단조·제련실은 없앰.
- 없앤 것: 공실·확장 블록, 연구실 B, 교역실(송신기는 남서 격납 갑판), 일반 창고, 단순작업실(가공실에 합침), 남쪽 포대, 대공 독립망. 보호막 5기(전용 방 2 + 금속·부품 작업실·예비 발전실·방어 설비실 안).
- 하부구조 21,700칸 / 지지 30,000 (구동기 + 확장기 2 + 반응로 3).
- 발전 1,516,720 W, 사용 252,495 W, 방어 스위치 끄면 −50,800 W.
- 포탑 47기, 전투기 8기, 구속대 30기, 수경재배기 36기, 수족관 7기, 폭발 쌍 800쌍 통과, 진입로 열림.
- 경비실 포탑(v24): 동측 경비실 8기를 킬존 옆 y46~88에 줄 배치(`EG_LINE`, `place_near`; 출구 쪽 화염→유탄→회전포→벙커→회전포→벙커→회전포→화염 입구 쪽). 북측 경비실 화염·회전포는 남쪽 총안 앞. 경비실 총안 벽 옆 열(x153)·바깥벽 옆 열(x157)은 예약 통로. 킬존 모든 칸이 경비실 포탑 8기 이상 사거리·시야 안(이전 입구 쪽 3~5기).
- 연결 설비(v26): `LINK_ROOMS` 방은 `autofill_room` 이 작업대·연결 설비를 빼 두고, 링크 단계에서 작업대 배치를 14가지 시험해 공유 효과가 가장 큰 배치를 고른 뒤 설비를 욕심쟁이로 채움(같은 방, 거리 안, 인접 조건, 대상당 최대 수, 1개당 +5% 이상). 나노 조립기는 표에 없어 조립 작업대 묶음을 씀(게임 화면 확인). 로봇 어시스턴트는 게임 연결 버그로 제외(`LINK_NEVER`). 150칸 넘는 링크 방은 `AISLE`(가운데 통로 + 문 앞 통로)에 건물을 못 놓음. 도면 라벨에 방·갑판 크기(가로×세로) 표시. 도면 '연결선' 버튼·툴팁.
- 게임 반영(v26): 온도 조절은 I.T.C.V.S.(CMC_WallTempController, 벽 설치·배기 없음, 방마다 1 + 150칸마다 1). 농장은 대형 자동 수경재배기 24기. 공용 침실 2실 → 개인실 4실(벽 x60). 조명은 게임에서 직접 넣음.
- 전선 2망: 본선(상시) / 방어망(빨강: 북쪽 포대 3곳 → 동쪽 외벽 밑 x=158 → 방어 진입 갑판, 방어 설비실 (157,12) 주 스위치 1개).

## 남은 일 / 아이디어

- 빠졌던 설비(v30): 룬 작업대·제련 작업대(무기고), 모에로틀 제약대·통천의·정수지주 2(약품 가공실, 원시 통천정·반혼로 제외), 밀리라 자원 설비(태양 수정 2·장막 수정·암흑 물질 구속기·태양꽃 2·토양 수경 2 → 밀리라 작업실, 태양 조리대 → 주방, 포집탑+반사판 4 → 북서 격납), 밀리라 확장 추출기 3종(채굴실).
- 게임 반영(v29, 스크린샷 자동 정합으로 칸 단위 확인): 북쪽 포대 3곳 y −16부터(북서 38×12, 북측 48×25, 북동 49×16), 북서 연결 통로 (1,−3,44,3), 북측 에어록 1 (40,−10,9,6) 바깥 문 (44,−11), 북측 전실 (46,−3,3,3), 서쪽 외곽 공실 5실 (x −6..−1)과 북서 격납 갑판 x −6까지, 충전 B (1,63,19,9)·채굴 (1,73,19,15), 거주 통로 북쪽 문 (73,9). 정합 도구는 스크래치패드 reg.py/diff.py(세션 밖에서는 없음).

- 가로 통로 1, 세로 통로 1·2 중간 킬존(옆 방 2~3줄 축소 필요) — 사용자 답변 대기였음.
- 가로 여유 1칸, 세로 약 27칸. 그리핀 전투기 자리 없음. 북쪽 포대 갑판은 확장 예비지.
- 식탁·의자·조명 등 가구는 도면에서 빠져 있음.
- 게임에서 확인할 값: 연결 거리 6칸과 동거리 우선순위, 반응로 거리 제한 설정, 고급 보호막 반경 설정, 진공 지붕·드롭 포드 동작.

## 작업 방식 메모

- 사용자는 한국어. 스크린샷 반복은 싫어함(필요할 때만). 큰 구조 변경 전에는 선택지를 짧게 보여 주고 고르게 함.
- 변경할 때마다 `build.py` 로 검증 통과 확인 → 설명 카드 문구(`page_template.html`) 맞추기 → 문서 번호(RW-GRAV-nn) 올리기 → 아티팩트 재게시.
